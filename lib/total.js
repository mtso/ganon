/**
 * `total` returns the sum of all its number arguments.
 *
 * @params {Numbers} Several number terms.
 * @returns {Number} The sum of all number arguments.
 */
function total() {
  throw new Error("Method is still a skeleton");
}

module.exports = total;
